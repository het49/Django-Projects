version: "3.8"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - sqlite_data:/app/db.sqlite3
      - media_data:/app/media

volumes:
  sqlite_data:
  media_data: